# Example variable values for local development / testing
# Copy to terraform.tfvars and fill in real values before running locally.

management_cluster_id = "mc01"
regional_id           = "regional"
enable_pitr           = false

app_code      = "infra"
service_phase = "dev"
cost_center   = "000"
