apiVersion: v1
stringData:
  # Pod Identity handles auth via dns-pod-identity module.
  # HyperShift requires this secret to exist; must be valid INI with no
  # profiles so the SDK falls through to Pod Identity credentials.
  credentials: "# pod-identity"
kind: Secret
metadata:
  name: external-dns
  namespace: hypershift
type: Opaque
