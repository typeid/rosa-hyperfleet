# Injected by ApplicationSet at runtime — defaults here for helm lint only
global:
  aws_region: ""
  management_clusters: ""

grafana:
  image:
    registry: docker.io
    repository: grafana/grafana-oss
    tag: "11.4.0"
    pullPolicy: IfNotPresent

  # FedRAMP: non-root (UID/GID 472)
  securityContext:
    runAsNonRoot: true
    runAsUser: 472
    runAsGroup: 472
    fsGroup: 472

  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 472
    seccompProfile:
      type: RuntimeDefault

  # Static password prevents ArgoCD out-of-sync diffs.
  # Login form is disabled; this value is not used for access.
  adminPassword: "admin"

  serviceAccount:
    create: true
    name: grafana

  "grafana.ini":
    server:
      root_url: "%(protocol)s://%(domain)s:%(http_port)s/"

    security:
      disable_gravatar: true

    database:
      type: sqlite3
      path: /var/lib/grafana/grafana.db
      wal: true

    auth:
      disable_login_form: true

    "auth.anonymous":
      enabled: true
      org_role: Admin

    "auth.basic":
      enabled: false

    users:
      allow_sign_up: false
      auto_assign_org_role: Admin

    analytics:
      reporting_enabled: false
      check_for_updates: false
      check_for_plugin_updates: false

    log:
      mode: console
      level: info

  service:
    type: ClusterIP
    port: 80
    targetPort: 3000

  persistence:
    enabled: false

  # SRE UI ALB — injected at runtime by ApplicationSet; empty when ALB is not deployed
  sre:
    targetGroup:
      arn: ""

  envFromConfigMaps:
    - name: grafana-region-env

  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: Thanos
          type: prometheus
          url: http://thanos-query-frontend-thanos-query.thanos:9090
          isDefault: true
          access: proxy
          editable: false
          jsonData:
            timeInterval: "30s"
            httpMethod: POST
        - name: RC Prometheus
          type: prometheus
          url: http://monitoring-prometheus.monitoring:9090
          isDefault: false
          access: proxy
          editable: false
          jsonData:
            timeInterval: "30s"
            httpMethod: POST
        - name: Loki
          type: loki
          url: http://loki-query-frontend.loki.svc:3100
          access: proxy
          editable: false
          jsonData:
            maxLines: 1000
        - name: CloudWatch Logs (Regional)
          type: cloudwatch
          access: proxy
          editable: false
          jsonData:
            authType: default
            defaultRegion: $AWS_DEFAULT_REGION
            logsTimeout: "15m"

  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi

  replicas: 1

  # Dashboards fetched from grafana.com at startup
  dashboards:
    default:
      node-exporter-full:
        gnetId: 1860
        revision: 37
        datasource: Thanos
      kubernetes-cluster-monitoring:
        gnetId: 7249
        revision: 1
        datasource: Thanos
      kubernetes-pods:
        gnetId: 6417
        revision: 1
        datasource: Thanos

  # Sidecar watches ConfigMaps labeled grafana_dashboard="1" and places them
  # in subdirectories based on the k8s-sidecar-target-directory annotation.
  # foldersFromFilesStructure creates Grafana folders from the subdirectory names.
  sidecar:
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 50m
        memory: 64Mi
    datasources:
      enabled: true
      label: grafana_datasource
      labelValue: "1"
      searchNamespace: grafana
      skipReload: true
      initDatasources: true
    dashboards:
      enabled: true
      label: grafana_dashboard
      labelValue: "1"
      searchNamespace: grafana
      folderAnnotation: k8s-sidecar-target-directory
      # k8s-sidecar 1.28.0 only supports basic auth for the reload call, which
      # is disabled in this deployment. skipReload stops the sidecar from calling
      # POST /api/admin/provisioning/dashboards/reload (which would 403).
      # Grafana's built-in file provisioner polls the dashboard directory on its
      # own interval and picks up changes without the explicit reload trigger.
      skipReload: true
      provider:
        foldersFromFilesStructure: true
        allowUiUpdates: false
